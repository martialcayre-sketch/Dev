# Local development environment variables
# ADMIN_API_KEY is configured as a Firebase Secret in production

# Base URL for patient app links in emails
# For local testing, use the dev server URL. For production, set this to your real domain.
# PATIENT_APP_BASE_URL=http://localhost:3020
PATIENT_APP_BASE_URL=https://neuronutrition-app-patient.web.app

# Migration secret for one-time data migration
# Use Firebase Secret in prod: MIGRATION_SECRET (set via `firebase functions:secrets:set`)
# For local dev, you can export it manually in your shell if needed:
# export MIGRATION_SECRET=temp-migration-secret-2024
