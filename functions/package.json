{
  "name": "functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "npm run build && firebase emulators:start --only functions",
    "watch": "tsc -p tsconfig.json --watch",
    "deploy": "npm run build && firebase deploy --only functions",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "express": "^5.1.0",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0",
    "zod": "^4.1.12",
    "@neuronutrition/shared-questionnaires": "file:./vendor/@neuronutrition/shared-questionnaires"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/jest": "^30.0.0",
    "@types/node": "24",
    "firebase-functions-test": "^3.4.1",
    "jest": "^30.2.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.7.0"
  }
}
